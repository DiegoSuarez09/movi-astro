---
interface Props {
  kicker: string
  title: string
  copy: string
  light?: boolean
}
const { kicker, title, copy, light = false } = Astro.props
---

<div class={`section-intro ${light ? 'intro-light' : ''}`}>
  <p class="eyebrow">{kicker}</p>
  <h2 set:html={title} />
  <p>{copy}</p>
</div>
